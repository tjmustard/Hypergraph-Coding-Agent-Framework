# Hypergraph Coding Agent Framework — Agent Instructions
# Applies to all Roo Code modes

Read `AGENTS.md` for all system mandates.

When a skill is invoked, read `.agents/skills/<name>/SKILL.md` and follow its instructions precisely.
